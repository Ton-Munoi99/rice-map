# Auto-generated backups
rice-data_bak_*.js

# Raw source PDFs (ไม่ขึ้น GitHub เพราะใหญ่)
*.pdf

# Python cache
scripts/__pycache__/
*.pyc
*.pyo

# Playwright / Lighthouse (ผลตรวจจากเครื่อง ไม่ใช่ของโปรเจกต์)
.playwright-cli/
.playwright-mcp/
.lighthouse-report.json

# Raw GeoJSON (ใหญ่ ~1.2MB — ใช้ thailand-data.js แทน)
thailand.json

# Raw source files สำหรับ build scripts (ใหญ่ / ไม่ต้อง commit)
data/source/

# CSV snapshot used by the static app at runtime

# OS files
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
*.code-workspace

# Node
node_modules/

# Claude Code session/config + worktrees (must not be committed)
.claude/
.firecrawl/
scratch/

# Scratch / working files (research, OCR, temp exports)
ocr-table-pages/
fertilizer-table-pages-rendered/
fertilizer-tables-by-page/
fertilizer-tables-by-page.xlsx
fertilizer-tables-by-page.zip
*.docx
*.xlsx
*.txt
app_check.js
# keep legit tracked deliverables
!rice_mills_distance_analysis.xlsx
