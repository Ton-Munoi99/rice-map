# Changelog

บันทึกการพัฒนา **Thailand Rice Intelligence Map** — แผนที่ข้าวรายจังหวัดของประเทศไทย
รวบรวมจาก git history (ไม่รวม commit อัปเดตข้อมูลอัตโนมัติรายวันจาก GitHub Actions)

รูปแบบอิง [Keep a Changelog](https://keepachangelog.com/) · โปรเจกต์นี้ deploy ต่อเนื่อง
(ไม่มีเลขเวอร์ชัน) จึงจัดกลุ่มตามเดือน · วันที่เป็น พ.ศ.

---

## สิงหาคม 2569

### ลบออก (Removed)
- **🌧️ ถอด layer "ฝนล่วงหน้า 48 ชม. (ละเอียด)" ของ TMD ออกทั้งหมด — เหลือ 20 layer**
  (layer, สคริปต์, workflow, ไฟล์ข้อมูล, monitor, เอกสาร · secret `TMD_API_TOKEN`
  เจ้าของ repo ลบเองที่ GitHub Settings)
  - **เหตุผลหลัก: มันขัดกับ layer "พยากรณ์ฝน 7 วัน" ที่อยู่ปุ่มติดกัน โดยไม่มีหลักฐาน
    ว่าอันไหนถูก** วัดช่วง 48 ชม. เดียวกัน 77 จังหวัด: TMD เฉลี่ย 6.0 มม. · Open-Meteo
    เฉลี่ย 31.4 มม. (ต่างกัน 5 เท่า) · **สหสัมพันธ์ r = 0.21** และ **5 จังหวัดฝนหนักสุด
    ของสองชั้นไม่ซ้ำกันสักจังหวัด** — จันทบุรี TMD ว่า 5.6 มม. OM ว่า 104 มม. คนดูกด
    สองปุ่มติดกันแล้วไม่รู้จะเชื่ออันไหน แย่กว่ามีชั้นเดียว
  - **วัดกับฝนที่ตกจริงแล้วไม่ได้ดีกว่ากัน** — เทียบกับสถานีวัดฝน สสน. 4,273 จุด ใน
    76 จังหวัด: TMD MAE 7.0 มม. r=0.39 (ทายต่ำไป เฉลี่ย 4.0) · Open-Meteo MAE 10.3
    r=0.41 (ทายสูงไป เฉลี่ย 14.6) เทียบกับวัดจริง 7.8 · **ความสามารถเรียงลำดับพอกัน**
    และวันที่ทดสอบ **ทั้งคู่พลาดระนองที่ฝนตกจริง 85 มม.** (ข้อมูลวันเดียว ไม่พอสรุปว่า
    ใครแม่นกว่า แต่พอสรุปได้ว่าไม่มีอันไหนแม่นพอที่จะให้อีกอันมาขัด)
  - **ป้าย "(ละเอียด)" ผิดมาตลอด** — "2 กม." คือความละเอียดของตัวโมเดล แต่ TMD คืนมา
    77 รายการต่อ 77 จังหวัด = **จังหวัดละจุดเดียว** ส่วน Open-Meteo สุ่ม 6 จุดต่อจังหวัด
    แล้วเอา p90 · บนแผนที่ระบายสีรายจังหวัด TMD จึงเป็นตัวแทนพื้นที่ได้**หยาบกว่า**
  - **ไม่ได้เพิ่มช่วงเวลา** — 48 ชม. อยู่ใน 7 วันอยู่แล้ว และไฟล์ Open-Meteo มีค่ารายวัน
    ครบ (`values[]`) ถ้าอยากได้ "48 ชม.แรก" คำนวณจากไฟล์เดิมได้ ไม่ต้องมีแหล่งที่สอง
  - **ไม่มีใครใช้ต่อ** — `TMD_DATA` ถูกอ่านโดย layer ตัวเองเท่านั้น ต่างจาก Open-Meteo
    ที่ป้อนทั้ง layer เตือนน้ำท่วมและ scoreboard วัดความแม่น
  - **ไฟล์ 204 KB ที่ 98% ไม่มีใครอ่าน** — เก็บฝนรายชั่วโมง 48 ค่าต่อจังหวัด แต่ tooltip
    โชว์แค่ยอด 24/48 ชม. ซึ่งเป็นรูปแบบเดียวกับ Open-Meteo เป๊ะ · ถ้าตัด hourly ออก
    ไฟล์เหลือ 5 KB · แปลว่าเราเก็บสิ่งเดียวที่ TMD ทำได้แต่ Open-Meteo ทำไม่ได้ไว้
    แล้วไม่เอามาแสดง
  - **ลิงก์เก่าที่แชร์ไว้ไม่พัง** — `#layer=tmdRain` ถูก whitelist ปัดตกแล้วกลับไป layer
    ตั้งต้น จังหวัดที่เลือกไว้ยังอยู่ hash ถูกเขียนใหม่ให้สะอาด ไม่มี error
  - **ยังเหลือฝน 3 ชั้นที่ตอบคนละคำถามชัดเจน:** ฝนที่ตกไปแล้ว (7 วันย้อนหลัง ดาวเทียม) ·
    ฝนที่จะตก (7 วันข้างหน้า) · ฝนที่วัดได้ตอนนี้ (สถานี realtime)
  - **ถ้าอนาคตอยากได้ "ฝนจะตกกี่โมง"** ซึ่ง Open-Meteo รายวันตอบไม่ได้จริงๆ — นั่นคือ
    กราฟรายชั่วโมงในการ์ดรายละเอียดจังหวัด ไม่ใช่ layer ระบายสีอีกอัน

### เปลี่ยน (Changed)
- **⬆️ อัป GitHub Actions ทุก workflow ให้พ้น Node.js 20 ที่ GitHub ประกาศเลิกใช้**
  `actions/checkout@v4 → v7` (19 จุด) · `actions/setup-python@v5 → v7` (18 จุด)
  - ตรวจก่อนอัป ไม่ได้เดาเลขเวอร์ชัน: อ่าน `runs.using` จาก action.yml ของแต่ละ tag
    จริง — checkout เป็น node24 ตั้งแต่ v5 · setup-python ตั้งแต่ v6 · ทั้งคู่ล่าสุดคือ v7
  - ไล่อ่าน release note ของทุก major ที่ข้าม หา breaking change ที่กระทบเรา แล้ว
    เช็คในรีโปว่าไม่โดน: v7 ของ checkout บล็อกการ checkout fork PR บน
    `pull_request_target`/`workflow_run` (เราไม่ได้ใช้ trigger สองตัวนี้) · v7 ของ
    setup-python ถอด input `pip-install` ออก (เรา `pip install` เป็น step แยกอยู่แล้ว)
  - diff เป็นบรรทัด `uses:` ล้วน 37 บรรทัด ไม่มีอย่างอื่นปน · YAML ผ่านทั้ง 20 ไฟล์
- **📌 เขียนกฎ `bkk_today()` ลง `AGENTS.md`** ให้เป็นข้อบังคับของรีโป ไม่ใช่ความรู้ที่
  ติดอยู่กับคนที่เพิ่งแก้ · ระบุแยกด้วยว่า "ป้ายวันที่" ใช้ `bkk_today()` แต่ "ตรรกะวันที่"
  ต้องคงเป็นเวลา runner และ 4 จุดนั้นมีคอมเมนต์กำกับแล้ว อย่าไปแก้
- **🕐 แก้ป้าย "อัปเดตเมื่อ" ที่ขึ้นเป็นเมื่อวาน — 16 สคริปต์** เปลี่ยนจาก `date.today()`
  (บน runner คือ UTC) มาเป็น `bkk_today()` ที่มีอยู่แล้ว · cron หลายตัวยิงช่วง
  17:00–24:00 UTC ซึ่งเป็นวันถัดไปแล้วตามเวลาไทย ป้ายบนเว็บจึงขึ้นย้อนหลังไปหนึ่งวัน
  - เพิ่ม `bkk_now()` ใน `riceutils.py` สำหรับ `fetched_at` ของเขื่อนที่ต้องมีเวลาด้วย
  - **ไม่ได้ไล่แทนทุกที่แบบเหมารวม** — 4 จุดที่เหลือเป็นตรรกะวันที่ ไม่ใช่ป้าย และ
    ตั้งใจให้เป็นเวลา runner: ขอบบนของช่วงวันที่ที่ขอจาก Open-Meteo (เลื่อนเป็นวันไทย
    จะกลายเป็นขอวันอนาคต), การคำนวณปีฤดูกาล, การจับคู่หน้าต่างพยากรณ์กับ GSMaP ที่เป็น
    UTC, และ fallback ของ pubDate ข่าวที่บรรทัดข้างบนแปลงเป็น UTC อยู่แล้ว —
    **เขียนคอมเมนต์กำกับไว้ทั้ง 4 จุด** กันคนมาไล่แก้ผิดรอบหน้า
  - ลบ import `date`/`datetime` ที่กลายเป็นของตายจากการแก้นี้ออกด้วย
- **🎨 แก้สี layer น้ำท่วมที่จางจนมองไม่เห็น** — ปลาย `lo` เดิมเป็นครีมอ่อน ซึ่ง layer
  ที่ไล่เฉดจริงๆ ใช้ได้ แต่ layer นี้มีแค่ 2 ค่า และ **13 ใน 14 จังหวัดเป็นค่าต่ำสุด**
  จังหวัดเกือบทั้งหมดจึงจมหายไปกับพื้นหลังเข้ม (เห็นตอนถ่ายภาพหน้าจอมาดูจริง) ·
  เปลี่ยน `lo` เป็นส้มเข้ม
- **🌊 เปลี่ยน layer "น้ำท่วมจริง" จากภาพดาวเทียม GISTDA → สถานีวัดระดับน้ำจริงของ สสน. (ThaiWater) + ข่าวเป็นบทสรุป** · ถอด GISTDA ออกทั้งหมด
  - **เหตุผล: เอาไปเทียบกับข่าวจริงแล้วไม่ผ่าน** (28 ส.ค. 69) — ภาพเป็นฉากเดิมค้างมา 3 วัน (ต้นทางไม่มีฟิลด์วันถ่ายภาพเลย ตรวจแล้วว่าเป็นที่ต้นทางไม่ใช่บั๊กเรา) · **ไม่มีน่านซึ่งเป็นข่าวน้ำป่าใหญ่สุดของช่วงนั้น** เพราะน้ำป่าลดก่อนดาวเทียมถ่ายรอบถัดไป · และ**ขึ้นสีชัยภูมิว่าท่วม ขณะที่ข่าวรายงานว่าวิกฤตแล้ง** · layer ที่ชื่อว่า "น้ำท่วมจริง" แต่พลาดน้ำท่วมจริงและขึ้นสีที่แล้ง แย่กว่าไม่มี
  - **ของใหม่: `scripts/fetch_flood_status.py` → `data/flood-status.json`** ต่อยอดจาก `water-level.json` ที่ดึงมาอยู่แล้ว **ไม่ยิง API เพิ่ม** · รันต่อท้าย `fetch_water_level.py` ใน workflow เดียวกันทุก 3 ชม. (ถ้าแยก job จะอ่านไฟล์สถานีที่เก่ากว่าที่เพิ่ง commit)
  - **เกณฑ์สี มาจากสถานีวัดล้วน:** 🔴 ท่วม = มีสถานีล้นตลิ่ง (ระดับ 5) ≥1 จุด · 🟠 ใกล้ล้นตลิ่ง = สถานีน้ำมาก (ระดับ 4) ≥3 จุด **และ** ≥30% ของสถานีในจังหวัด — เงื่อนไข "หลายจุด + สัดส่วน" กันสถานีเดียวโดดๆ ลากทั้งจังหวัดเป็นสีท่วม · เกณฑ์แรกที่ลอง (`≥3 จุด` **หรือ** `≥30%` **หรือ** `≥1 จุด`) ให้ 47/77 จังหวัด ทิ้งไปเพราะเป็นปัญหา "เตือนทุกจังหวัด" แบบเดียวกับที่เพิ่งแก้ไปในเตือนภัยฝน
  - **วัดกับข้อมูลจริงก่อนเปิดใช้:** ได้ **14/77 จังหวัด** (ท่วมจริง 1) และกลุ่มเจ้าพระยาที่ออกมา — อยุธยา ลพบุรี สุพรรณบุรี สมุทรปราการ กทม. — **ตรงกับรายชื่อที่ ปภ. เตือนวันก่อนหน้าพอดี**
  - **ข่าวเป็นบทสรุปประกอบ ไม่มีผลกับสีเด็ดขาด** · เหตุผลที่ไม่ให้ข่าวคุมสี: พาดหัวหลอกได้ — ข่าว "ชัยภูมิวิกฤตแล้ง นาข้าวตายนับแสนไร่" มีคำว่า "น้ำท่วม" อยู่ในเนื้อหา · ระหว่างทำเจอสองบั๊กและแก้แล้ว: query รวมคำว่า "น้ำท่วม" คืนข่าวเนปาล/ทิเบตเกือบทั้งหมดจนแมตช์จังหวัดไทยไม่ได้เลย (แก้เป็นถามแยกรายจังหวัด) · Google แมตช์คำค้นกับ*เนื้อข่าว* ไม่ใช่พาดหัว ทำให้ข่าวกลิ่นเคมี/ยาเสพติด/อุบัติเหตุที่บังเอิญมีชื่อจังหวัดหลุดเข้ามา (แก้เป็นบังคับให้พาดหัวมีคำเรื่องน้ำด้วย เหลือ 5 ข่าวและตรงเรื่องทั้งหมด)
  - **ถอดออก:** `scripts/fetch_gistda_flood.py`, `.github/workflows/update-gistda-flood.yml`, `data/gistda-flood.json`, ตัวเลข "นาข้าวจมน้ำ" ที่คำนวณด้วย GEE (แม่นได้ไม่เกินพื้นที่น้ำท่วมที่เป็นต้นทาง), option `updatedVerbTh/En` ใน `layerMeta` ที่ไม่มีใครใช้แล้ว
  - **สิ่งที่ layer นี้มองไม่เห็น เขียนบอกไว้ในหน้าเว็บตรงๆ:** จังหวัดที่ไม่ขึ้นสี = ไม่มีสถานีเข้าเกณฑ์ **ไม่ใช่ยืนยันว่าไม่ท่วม** — น้ำท่วมขังนอกลำน้ำไม่มีสถานีวัด

### เพิ่ม (Added)
- **🌊 Layer ใหม่ "น้ำท่วมจริง (ดาวเทียม)" จาก GISTDA — layer ที่ 21** · ได้ไอเดียจากการอ่านโปรเจกต์ [SIAHRA](https://github.com/flukelaster/SIAHRA) ซึ่งใช้แหล่งนี้อยู่
  - **เติมช่องว่างที่วัดได้จริง ไม่ใช่เดา** — เตือนภัยน้ำท่วมเดิมของเราคำนวณจาก*พยากรณ์ฝน* เทียบกับ GISTDA ที่เห็น*น้ำที่ท่วมอยู่แล้ว* วันที่ 25 ส.ค. 69: GISTDA พบน้ำท่วม 13 จังหวัด เตือนภัยเราจับได้ 12 แต่ **พลาดนครราชสีมาซึ่งท่วมหนักที่สุด** (62 ตำบล 46,178 ไร่ · เราให้แค่ 🟡) เพราะโคราชท่วมจากน้ำที่ไหลมาแล้ว ไม่ใช่ฝนที่กำลังจะตก — เป็นข้อจำกัดเชิงโครงสร้างแบบเดียวกับที่เจอตอนตรวจข่าวน้ำป่าน่าน แต่คราวนี้มีตัวเลขยืนยัน · **สอง layer นี้ตอบคนละคำถาม ใช้คู่กัน ไม่ใช่แทนกัน**
  - `scripts/fetch_gistda_flood.py` → `data/gistda-flood.json` (54 KB, ตัด geometry ออก เก็บแต่ตัวเลข) · WFS สาธารณะ **ไม่ต้องมี API key ไม่มีค่าใช้จ่าย** · รายตำบล รวมเป็นรายจังหวัด/อำเภอ · cron ทุก 6 ชม.
  - **ต้นทางมี 2 กับดัก แก้ไว้แล้วทั้งคู่** (ยืนยันด้วยการยิงจริง): (1) **ไม่มีวันที่ถ่ายภาพเลย** ทั้งใน property และ GetCapabilities — จึงทำลายนิ้วมือฉาก (hash ของชุดตำบล+พื้นที่) ไว้บอกว่า"เห็นภาพชุดนี้ครั้งแรกเมื่อไหร่" และ**หน้าเว็บไม่เขียนว่า "น้ำท่วมวันนี้"** เด็ดขาด · (2) **ข้อความไทยเป็น cp874 ที่ถูกติดป้ายผิดเป็น UTF-8** (`µ.à·¾ÒÅÑÂ` = `ต.เทพาลัย`) ถอดกลับแล้ว
  - **จังหวัดที่ไม่ขึ้นสี = ไม่พบในภาพล่าสุด ไม่ใช่ยืนยันว่าไม่ท่วม** — `valueFn` คืน `null` ไม่ใช่ `0` และ tooltip ของจังหวัดที่ไม่ท่วมเขียนบอกตรงๆ ว่า GISTDA เผยแพร่เฉพาะพื้นที่ที่ตรวจพบ
  - tooltip แสดง**อำเภอที่ท่วมหนักสุด 3 อันดับ** (เช่น โคราช: อ.พิมาย 12,672 ไร่ · อ.โนนสูง 5,761 · อ.เมืองยาง 4,836)
  - **เพิ่มเข้า `check_data_freshness.py` ตั้งแต่วันแรก** — ไม่ปล่อยให้ซ้ำรอย `tmd-forecast.json` ที่หลุด monitor ไปจนต้องมาเจอตอน stale-check
  - ทดสอบในเบราว์เซอร์กับข้อมูลจริง: 13/77 จังหวัดขึ้นสี, deep-link `#layer=floodExtent` โหลดตรงได้จากหน้าเปล่า, sweep ครบ 21 layer ไม่มีตัวไหนพัง, console ว่าง
  - **🌾 ต่อยอด: ตัดพื้นที่น้ำท่วม × mask นาข้าว → "นาข้าวจมน้ำกี่ไร่"** (สิ่งที่ Rice Map ทำได้ดีกว่า SIAHRA เพราะมีชั้นข้าวอยู่แล้ว) · ผลรอบแรก **น้ำท่วม 359,633 ไร่ · เป็นนาข้าวประมาณ 177,574 ไร่ (49%)**
    - **วัดความน่าเชื่อถือก่อนตัดสินใจว่าจะขึ้นเว็บไหม** ไม่ได้เขียนแล้วปล่อย: GEE สุ่มได้ **98.4%** ของพื้นที่ที่ GISTDA แจ้ง (scale 250 ม. · ที่ 1 กม. ได้ 94.1%) · ผลนิ่งข้าม scale · **สัดส่วนนากระจาย 18.9–74.6% ตามภูมิศาสตร์จริง** (มหาสารคาม 75% · หนองคาย 19%) ซึ่งเป็นสัญญาณว่า mask มีข้อมูลจริง ไม่ใช่คืนค่าคงที่
    - **ติดป้ายตามความจริง** — `rice_kind: "derived"` แยกจากพื้นที่น้ำท่วมที่เป็น `observed` · หน้าเว็บเขียน **"ประมาณ"** และ **"น่าจะต่ำกว่าจริง"** ทุกที่ เพราะ mask จับนาได้ ~0.70 เท่าของ สศก. · เขียนกำกับด้วยว่า **"ไม่ใช่ตัวเลขสำหรับเคลมชดเชย"**
    - **scene fingerprint ที่ทำไว้ได้ใช้ประโยชน์รอบสอง** — คำนวณ GEE เฉพาะตอนฉากเปลี่ยน (ฉากเดิมข้ามไป ประหยัด ~5 นาที/รอบ) ทดสอบแล้วรันซ้ำข้ามจริงและค่าไม่เพี้ยน
    - **GEE พังไม่ทำให้ข้อมูลน้ำท่วมพัง** — degrade เป็น `rice_rai: null` พร้อมบันทึกสาเหตุใน `rice_error` ไม่เดาค่าแทน
    - เจอข้อจำกัด GEE ระหว่างทาง: payload จำกัด 10 MB แต่ polygon ทั้งชุด 25 MB → ปัดพิกัดเหลือ 5 ตำแหน่ง (~1 ม.) + แบ่งส่งชุดละ 25
- **🌧️ Layer ใหม่ "ฝนล่วงหน้า 48 ชม. (ละเอียด)" จาก TMD (ตัวเลือก C ที่ค้างไว้ตั้งแต่ 11 ส.ค.)** — เสริมเตือนภัยน้ำท่วม 7 วันที่มีอยู่ ไม่ได้แทน
  - `scripts/fetch_tmd_forecast.py` ดึงจาก TMD NWP API endpoint `/hourly/region` — 6 ครั้ง (รายภาค) แทนยิงทีละ 77 จังหวัด, รวมฝนเป็น `rain_48h_mm`/`rain_24h_mm` ต่อจังหวัด
  - **แก้ความเข้าใจผิดเรื่องระยะเวลา** — เอกสาร TMD ระบุ hourly forecast สูงสุด **48 ชั่วโมง** ไม่ใช่ 72 ชม. ตามที่เข้าใจไว้ก่อนหน้า (เดิมเรียก "ฝน 3 วันข้างหน้า") label ที่ถูกต้องคือ "48 ชม. / 2 วัน"
  - **เจอ response key ที่ไม่ตรงเอกสารระหว่างทดสอบจริงบน runner** — เอกสาร TMD เขียนไว้เอง 2 แบบ (`WeatherForecast` / `WeatherForcasts` สะกดผิด) แต่ของจริงคือคีย์ที่ 3 `WeatherForecasts` (สะกดถูก มี s) — ใช้ debug log จับได้จากรอบทดสอบที่ 2 แล้วรับทุกคีย์ที่เป็นไปได้กันเหนียว
  - ย้าย `PROVINCE_MAP` (จังหวัดไทย→อังกฤษ 77 รายการ) จาก `fetch_miller_prices.py` ไป `riceutils.PROVINCE_TH_EN` ที่เดียว — TMD คืนชื่อจังหวัดภาษาไทยเหมือนกัน ใช้ตัวเดียวกันได้ ไม่ต้องพิมพ์ซ้ำ
  - `.github/workflows/update-tmd-forecast.yml` — ทดสอบ token บน runner จริงผ่านแล้ว (77/77 จังหวัด) เปิด cron ทุก 6 ชม.
  - **layer บนแผนที่** ต่อผ่านระบบ config-driven ที่ ponytail-audit วางไว้ (`valueFn`/`palette`/`sourceTh` รวมจุดเดียว) จุดที่เหลือเป็น UI เฉพาะ 17 จุด (การ์ดรายละเอียด, tooltip, ป้ายอันดับ, ปุ่มปี) ตามแบบ `gsmap`/`forecast` — ทดสอบกับข้อมูลจริงในเบราว์เซอร์: 77 จังหวัดระบายสี, deep-link `#layer=tmdRain` โหลดตรงได้, console ไม่มี error
- **🧹 ตัดของที่ไม่ได้ใช้ทั้ง repo (จาก `/ponytail-audit`)** — รวม **−1,307 บรรทัด**
  - **ตัด layer ที่ไม่มีปุ่มให้กด 4 ตัว** (`ndvi` `rain` `rainfall` `drought`) เข้าถึงได้ทางเดียวคือพิมพ์ `#layer=…` ใน URL · ทั้งสี่มี layer ที่ดีกว่าทำหน้าที่แทนแล้ว (`riceEvi` · `gsmap` · `forecast` · `alerts`) · **ใส่ปุ่มให้ `profit` แทนการลบ** เพราะไม่มีตัวแทนและเพิ่งตัดสินใจเก็บไว้เมื่อวาน — ตอนนั้นยังไม่รู้ว่ามันไม่มีปุ่ม
  - **ตามเก็บสาขา render ที่ตายตาม layer ที่ลบไป** — ตัด layer ออกจาก `layerMeta` แล้วแต่โค้ดวาดผลยังมี `if (layer === "ndvi")` / `layer === "rain" ? … :` กระจายอยู่ **36 จุด** ทั้งการ์ดรายละเอียด ป้ายอันดับ หมายเหตุแหล่งข้อมูล และ drill-down รายอำเภอ — เงื่อนไขที่เป็นจริงไม่ได้อีกแล้ว แต่ยังอ่านเจอเวลาไล่โค้ด (−193 บรรทัด)
    - **ใช้การจับคู่ตามย่อหน้า ไม่ใช่นับวงเล็บปีกกา** — รอบแรกเขียนตัวนับปีกกาแล้วมันไปสะดุด regex `en.replace(/'/g, "\'")` เข้าใจว่า `'` คือเปิดสตริง เลยกลืนปีกกาปิดของฟังก์ชันไปด้วยจนไฟล์พัง `SyntaxError` · ถอยกลับแล้วเปลี่ยนวิธี ตัดทีละชุดพร้อมเทียบ 19 layer กับ baseline ทุกครั้ง 4 รอบ
    - **สิ่งที่จงใจเก็บไว้ ไม่ใช่โค้ดตาย** — `alertsProv.level === "drought"` และ `w.type` คือ*ระดับเตือนภัย* ไม่ใช่ชื่อ layer · `weatherValueOf(en, "drought")` การ์ดรายละเอียดยังใช้คำนวณค่าเบี่ยงเบนฝน · `fetchLayerData("rain-daily.json", "RAIN_DATA", …)` การ์ดฝนยังอ่านอยู่
  - **กราฟแนวโน้มพังเงียบๆ เมื่อ layer มีข้อมูลปีเดียว** — ครัวเรือนเกษตรกรมีปีเดียว `idx / (length - 1)` จึงเป็น `0/0` = `NaN` ไหลลงพิกัด SVG · กราฟหายไปเฉยๆ ไม่มี error ให้เห็น ตอนนี้ขึ้นข้อความว่ายังไม่มีแนวโน้มให้เทียบแทน
  - **ผลลัพธ์: `layerMeta` 19 รายการ = ปุ่ม 19 ปุ่มพอดี** ไม่มีตัวไหนกำพร้าอีก
  - **ตัดท่อ NDVI ทั้งชุด** — `fetch_ndvi.py` + `fetch_ndvi_district.py` (288 บรรทัด) · 2 ขั้นตอนใน cron รายเดือน · `ndvi.json` + `ndvi-district.json` ที่โหลดทุกครั้งที่เปิดหน้า
  - **ยก mask/phenology + ค่าคงที่จูนเกณฑ์ไป `riceutils` ที่เดียว** — เดิมซ้ำสองไฟล์ และ **18 ส.ค. ต้องแก้ `MIN_EVI_MAX` สองที่พร้อมกัน** ถ้าหลุดที่เดียวแผนที่จังหวัดกับรายอำเภอจะใช้เกณฑ์คนละชุดโดยไม่มีใครรู้ · `fetch_rice_evi.py` 609→365 · `_district` 310→198 บรรทัด
  - **`LAYER_NO_TIMESERIES` → ธง `noTimeseries` ในตัว layer เอง** — รายชื่อคู่ขนาน 11 รายการที่ต้องแก้ตามทุกครั้งที่เพิ่ม layer และถ้าลืมจะเงียบ
  - ลบสคริปต์กำพร้า `build_households.py` (PROVINCE_HOUSEHOLDS สร้างจาก CSV ตอนรันแล้ว) และ `build_mills_distance_excel.py` (วิเคราะห์ครั้งเดียวจบ) · ยก `km_outside` ที่ซ้ำสองไฟล์ · เลิกใช้ `urllib` ในสคริปต์ที่มี `requests` อยู่แล้ว
  - **ตรวจทุกขั้น:** เทียบ 19 layer กับ baseline ก่อนแก้ ระบายสีเท่าเดิมทุกตัว · รัน pipeline จริงแล้วเทียบข้อมูล **890 รายการเหมือนเดิมทุกค่า** · ทดสอบเส้นทาง Firecrawl บน runner จริง
  - *ไม่ลบ `build_oae_rice_data.py` แม้ audit จะเสนอ — `build_naprang_data.py` import ฟังก์ชัน parse จากมัน (AGENTS.md เตือนไว้ชัด ผมอ่านข้ามเองรอบแรก)*
- **🛰️ ข้อมูลดาวเทียมสดขึ้นจาก 2 เดือน → 6 วัน** — เปลี่ยนค่า EVI ปัจจุบันจาก MOD13A3 (composite รายเดือน) ไป **MOD13Q1 (ราย 16 วัน 250 ม.)** · หน้าเว็บแสดงช่วงจริง "12 ก.ค. – 12 ส.ค. 2569" แทนเลขเดือน
  - **ต้นเหตุที่ช้า 2 เดือนไม่ใช่ดาวเทียม แต่เป็น cron** — ตั้งไว้วันที่ 8 ซึ่ง MOD13A3 ของเดือนก่อนยังไม่เข้า GEE (log: `No data for 2026-07 → fallback to 2026-06`) · เลื่อนเป็นวันที่ 20 แก้ได้ทันทีเหลือ ~1 เดือน แล้วจึงเปลี่ยนสินค้าดาวเทียมต่อ
  - **เจอปัญหาระหว่างทางและแก้ 2 รอบ:** composite ราย 16 วันมีภาพให้เลือกน้อยกว่ารายเดือน หน้าฝนไทยจึงเหลือพิกเซลที่เมฆบังเยอะ — รอบแรกค่าเฉลี่ยประเทศร่วง 0.43→0.33 และ 40 จังหวัดถูกจัดเป็น "ทรงพุ่มโรย" กลางเดือน ส.ค. ซึ่งเป็นไปไม่ได้ · กรอง `SummaryQA ≤ 1` แก้อคติได้แต่ครอบคลุมเหลือ 51/77 จังหวัด · รวม 2 composite (~32 วัน) แล้วเฉลี่ยข้ามภาพจึงอุดรูได้
  - **ผลสุดท้ายเทียบของเดิม:** ครอบคลุม 70 จว. (เดิม 72) · EVI เฉลี่ย 0.430 (เดิม 0.431) · แต่ข้อมูลจบที่ 12 ส.ค. แทน 31 ก.ค. และอัปเดตทุก 16 วันแทนรายเดือน
  - mask/phenology ยังใช้ MOD13A3 รายเดือนย้อน 12 เดือนเหมือนเดิม (เป็นค่าสะสมทั้งปี ความสดไม่สำคัญ) การนับพื้นที่จึงยังอยู่บน grid 1 กม. ค่าคงที่ 625 ไร่/พิกเซลไม่เปลี่ยน
- **🌊 ระดับน้ำแม่น้ำโขง (MRC)** — อุดช่องโหว่ที่บันทึกไว้ในหมวดทราบปัญหา · ดึง 31 สถานีบนโขงสายหลัก (ไทย 9) พร้อม **เกณฑ์เตือน/วิกฤตรายสถานี** จาก Mekong River Commission (API เปิด ไม่ต้องมี key อัปเดตทุก 15 นาที) แสดงเป็นการ์ดในจังหวัดริมโขง
  - **ทำไมสำคัญ:** layer เตือนน้ำท่วมคำนวณจากฝน แต่โขงขึ้นจากน้ำต้นทาง จังหวัดริมโขงท่วมได้ทั้งที่ฝนไม่ตก — ระบบเดิมมองไม่เห็นเลย · ตอนเพิ่มเข้ามา บึงกาฬอยู่ที่ **เกินระดับวิกฤต 0.28 ม.** และนครพนมเหลืออีก 0.12 ม. ซึ่งไม่ปรากฏที่ไหนในเว็บมาก่อน
  - `floodStage` ของนครพนม = 12.0 ตรงกับ "ระดับวิกฤต 12.00 เมตร" ที่ข่าวราชการอ้าง — เกณฑ์ชุดเดียวกัน
  - แมปสถานี→จังหวัดจากพิกัดจริง · เจอระหว่างทางว่าบึงกาฬไม่มี bbox (แยกจากหนองคายปี 2554 หลังชุด GAUL) และ bbox หนองคายยังกินพื้นที่บึงกาฬ ทำให้สถานีบึงกาฬถูกแมปเป็นหนองคาย — แก้ด้วยรูปหลายเหลี่ยมที่ `riceutils` มีอยู่แล้ว + เลือกกรอบที่จำเพาะกว่าเมื่อซ้อนกัน
- **🗺️ ลิงก์ดูความเหมาะสมที่ดินปลูกข้าว (LDD/Agri-Map)** — การ์ดในรายละเอียดจังหวัด + หน้าชาวนา · **ลิงก์ออกอย่างเดียว ไม่ดึงข้อมูลเข้ามา** เพราะชุดข้อมูลเป็น CC BY-NC-ND (ห้ามพาณิชย์ + ห้ามดัดแปลง — การรวมยอดตำบล→จังหวัดเข้าข่ายดัดแปลง) · บอกล่วงหน้าว่า Agri-Map ต้องสมัครฟรี พร้อมลิงก์ LDD ที่ไม่ต้องสมัครเป็นทางเลือก
- **📄 LICENSE + ตารางสัญญาอนุญาตรายแหล่ง** — แยกให้ชัดว่าโค้ดเป็น MIT ส่วนข้อมูลเป็นของหน่วยงานต้นทาง (ดู DATA_SOURCES.md)
- **📈 แนวโน้มระดับน้ำ (ม.รทก.) ในหมุดสถานี** — 628 สถานีที่ต้นทางไม่ให้เกณฑ์ตลิ่ง ตอนนี้แสดงว่ากำลังขึ้น/ลงกี่เมตร แทนที่จะไม่แสดงอะไรเลย *(ตอนแรกเข้าใจว่ากลุ่มนี้รวมสถานีริมแม่น้ำโขงด้วย — ตรวจพิกัดแล้วพบว่าไม่ใช่ ดูหมวดแก้ไข)*
- **📊 ตารางระยะห่างโรงสี (Excel)** — วิเคราะห์ 903 โรงสี geocode พิกัด (tambon/district, validate ว่าตกอยู่ในจังหวัดที่ถูกต้องจริง), คำนวณระยะห่างโรงสี-โรงสี (แยกค่า "นับทุกแห่ง" กับ "เฉพาะพิกัดต่างกัน" กันความเข้าใจผิดว่าโรงสีที่แชร์พิกัดตำบลเดียวกัน = อยู่ที่เดียวกัน) และระยะห่างจากตัวเมือง

- **🌾 หน้าชาวนา: คำแนะนำแยกตามระยะข้าว + การ์ดราคา/ความชื้น/จุดคุ้มทุน** — เดิม "ควรทำอะไรตอนนี้" ดูแค่ระดับเตือนน้ำท่วม/แล้ง จึงพูดเหมือนกันหมดไม่ว่าข้าวจะเพิ่งหว่านหรือใกล้เกี่ยว ซึ่งเป็นคนละเรื่องกัน (กล้าเล็กจมน้ำตายง่ายที่สุด แต่ข้าวใกล้แก่ต้องรีบเกี่ยว) · ตอนนี้ตัดสินจาก **ระยะข้าว × สภาพน้ำ**
  - เพิ่มการ์ด **ราคาข้าวเปลือกในจังหวัด** (ความชื้น 15% / 25%) จาก `prices-live.json` ที่มีอยู่แล้วแต่ไม่เคยถูกใช้ · มีแค่ 29/77 จังหวัด จังหวัดที่ไม่มีเขียนตรงๆ ว่าไม่มี **ไม่เอาค่าเฉลี่ยประเทศมาแสดงแทน**
  - **ตัดตัวเลขที่คำนวณต่อออกทั้งหมดก่อนขึ้นจริง** (ดูหมวดแก้ไข) — เหลือแสดงราคาดิบอย่างเดียว
  - ติดป้าย "ข่าวที่กำลังพูดถึง — **ยังไม่ใช่ประกาศทางการ**" คร่อมรายการข่าวเงินช่วยเหลือ · พาดหัวช่วงนี้เป็นการคาดการณ์แทบทั้งหมด ("ลุ้นได้ 40,000 บาท/ครัวเรือน") อ่านผ่านๆ แล้วนึกว่าเงินออกแล้ว — ป้ายอยู่เหนือรายการ ไม่ใช่หมายเหตุตัวเล็กข้างล่างที่ไม่มีใครอ่าน

### เปลี่ยนแปลง (Changed)
- **เกณฑ์เตือนน้ำท่วมเปลี่ยนจากตัวเลขคงที่ → ทวีคูณของฝนปกติรายจังหวัด (แผน A)** — เดิมทุกจังหวัดใช้ไม้บรรทัดอันเดียวกัน (🔴 ≥120 มม./7วัน) ทั้งที่ฝนปกติแต่ละจังหวัดต่างกันมาก (120 มม. = 1.2 เท่าของสัปดาห์ปกติที่ตราด แต่ = 2.8 เท่าที่นครราชสีมา) ทำให้บางวันติดเตือนพร้อมกันเกินครึ่งประเทศ ความหมายของสีแดงจางลง
  - เกณฑ์ใหม่ = **2.0x / 1.0x / 0.5x ของฝนปกติรายสัปดาห์หน้านาปี** (คำนวณจาก `weather-forecast.json` `forecast_rainfall_mm` ÷ 26 สัปดาห์) แทนคงที่ 120/60/30 มม. — สัดส่วน 4:2:1 เดิมยังคงไว้เหมือนกันทุกระดับ · ผู้ใช้เลือกทวีคูณ 2.0x เอง (จำลองไว้ก่อนแล้วว่าเทียบกับคงที่ 120มม. จะเตือนน้อยกว่า)
  - จังหวัดไม่มีข้อมูลค่าปกติ หรือฝนปกติต่ำผิดปกติ (<5 มม./สัปดาห์) ใช้เกณฑ์คงที่เดิมแทน (fallback) — ตอนนี้ทั้ง 77 จังหวัดมีข้อมูลค่าปกติครบ ไม่มีจังหวัดไหนใช้ fallback
  - ทดสอบกับข้อมูลจริง 20 ส.ค.: **10 จังหวัดเปลี่ยนระดับ** เทียบกับเกณฑ์คงที่เดิม เช่น สุรินทร์/ราชบุรี (ฝนปกติต่ำ) เลื่อนขึ้นเป็น 🔴 ส่วนพังงา/นครนายก (ฝนปกติสูง) ลดลงเป็น 🟠 — จำนวน 🔴 รวมบังเอิญเท่าเดิม (42) เพราะวันนี้ฝนตกหนักกระจายทั่วประเทศจริง แต่**จังหวัดไหนติดเปลี่ยนไปตามความเป็นธรรมของแต่ละพื้นที่**
  - `score_alerts.py` **ไม่แตะ** ยังวัด skill ของพยากรณ์เทียบฝนจริงที่ 120/30 มม. คงที่เหมือนเดิม เพราะเป็นคำถามคนละเรื่อง (พยากรณ์แม่นแค่ไหน vs. เกณฑ์ไหนควรขึ้นสีอะไร)
- **แยกปฏิทินข้าวเป็น `rice-season.js` ใช้ร่วมสองหน้า** — เดิมอยู่ใน `index.html` ที่เดียว หน้าชาวนาจึงไม่รู้ระยะข้าวเลย · ย้ายออกมาแทนการก๊อปวาง เพื่อไม่ให้มี 2 ที่ที่ลืมแก้แล้วสองหน้าบอกไม่ตรงกัน · เพิ่มค่า `stage` (planting/growing/harvest/offseason/prep) ไว้ให้โค้ดตัดสินใจแทนการอ่านข้อความ · ยุบ `SOUTH_PROVINCES` ที่ซ้ำกับ `REG.south` เป๊ะทั้ง 14 จังหวัด · self-check รันด้วย `node rice-season.js`
- **เตือนภัย: เปลี่ยนจากนับจำนวนจังหวัด → จัดอันดับความรุนแรง** — เดิมป้ายขึ้น `🔴26 🟠18 🟡14` ทั้งที่ 67/77 จังหวัดติดเตือน ซึ่งไม่ช่วยตัดสินใจ · ตอนนี้ขึ้น "เสี่ยงสูงสุดวันนี้: หนองคาย 402 · พะเยา 308 · บึงกาฬ 306 มม./7วัน" และเรียงลำดับด้วยปริมาณฝนจริง (เดิมเรียงด้วย level_num ที่มีแค่ 0–3 พอเสมอกันเป็นสิบจังหวัดลำดับจึงมั่ว) · **ไม่แตะเกณฑ์และสีแผนที่** เพื่อไม่ให้การทดลอง calibration ที่รันอยู่ปนกัน
- **README/DATA_SOURCES: แยกสัญญาอนุญาตโค้ดกับข้อมูล** — เดิมเขียน "MIT License" ลอยๆ ซึ่งผิดอยู่แล้วก่อนหน้านี้ เพราะข้อมูลพยากรณ์อากาศมาจาก Open-Meteo Free API ที่**อนุญาตเฉพาะการใช้ที่ไม่แสวงหากำไร** กระทบ 5 ไฟล์และทุก layer ที่คำนวณต่อ (รวมเตือนน้ำท่วม) · เพิ่มตารางสัญญาอนุญาต 11 แหล่ง แหล่งที่หาไม่เจอเขียนว่า "ไม่ได้ระบุ" ไม่ใช่เดาว่าใช้ได้
- **ผ่อนคลายฟิลเตอร์ข่าวให้รับข่าวปุ๋ยที่เกี่ยวข้าว** — ข่าวส่วนลดปุ๋ยส่วนใหญ่เขียน "เกษตรกร" กว้างๆ ไม่ใช่ "ข้าว/ชาวนา" ตรงๆ แม้บริบทจะเป็นนาข้าวจริง เพิ่มเงื่อนไข ปุ๋ย + คำบ่งชี้เกษตรกร/ต้นทุนการผลิต ควบคู่กับเกณฑ์เดิม
- **ตัดการ์ดราคา/จุดคุ้มทุนออกจากหน้าชาวนา** (farmer.html) ตามคำขอผู้ใช้

### แก้ไข (Fixed)
- **ปิด auto-calibration ในเตือนน้ำท่วม — วัดผลจริงแล้วแย่ลง** — 4 ส.ค. เพิ่มการหัก bias สะสม (ฝนที่ Open-Meteo ทายเกินจริงเป็นระบบ ~+50 มม.) ออกจากพยากรณ์ก่อนตัดสินระดับเตือน 🔴 เพื่อลด false positive · 20 ส.ค. `alert-scoreboard.json` สะสมครบ 5 windows ให้วัดผลได้จริงเป็นครั้งแรก — **precision แย่ลง 25.4%→12.7% และ recall แย่ลง 87.3%→77.8% พร้อมกันทั้งคู่** ไม่ใช่ trade-off ปกติที่ยอมรับได้ จึงปิดกลับไปใช้ฝนดิบทันทีแทนที่จะรอสะสมข้อมูลเพิ่ม เพราะทุกวันที่เปิดค้างไว้คือเตือนภัยแย่ลง
  - `load_forecast_bias()` คืน `0.0` เสมอ · เครื่องวัดผล (`score_alerts.py`, `alert-scoreboard.json`) ยังทำงานต่อเผื่อกลับมาเปิดพร้อมข้อมูลมากขึ้นในอนาคต
  - sample ที่วัดยังเล็ก (5 windows, tp=7) ตัวเลขอาจสุ่มผันผวนได้ แต่ไม่มีสัญญาณบวกด้านไหนเลย จึงไม่รอ
 ตัดพื้นที่นาจริงทิ้ง 9.2 ล้านไร่ — ถอดออก** — กฎเดิม: ถ้า cropland-bonus เกิน 3 เท่าของ GLAD ให้เชื่อ GLAD อย่างเดียว ตั้งใจกันยาง/ปาล์มที่หลุดมาใน union
  - **วัดจริงพบว่ากฎทำงานเฉพาะ 10 จังหวัด และเป็นจังหวัดข้าวใหญ่ทั้งหมด** (นครสวรรค์ · อยุธยา · ร้อยเอ็ด · พิจิตร · สุรินทร์ · ศรีสะเกษ · มหาสารคาม · ชัยนาท · นครนายก · นครปฐม) ซึ่งไม่มียาง/ปาล์ม · ส่วนจังหวัดยาง/ปาล์มที่กฎตั้งใจกัน **ไม่เคยเข้าเงื่อนไขเลยสักจังหวัด** (ทุกจังหวัดกลุ่มนั้นใช้ `union` อยู่แล้ว)
  - **อยุธยาเหลือ 6,875 ไร่ ทั้งที่ OAE มี 811,742 ไร่** เพราะ GLAD จับนาไม่ครบเอง — กฎเลยตีความว่า bonus ที่มากคือ "น่าสงสัย" ทั้งที่จริงคือ GLAD ขาด
  - **ผลจริงหลังถอด:** พื้นที่รวม 21.47 → 30.69 ล้านไร่ (0.49× → **0.70×** ของ OAE) · คลาดกลาง 53% → **43%** · คลาดเฉลี่ย 60% → **51%** · **ดีขึ้น 10 จังหวัด แย่ลง 0** · อยุธยา 783,750 (OAE 811,742) · นครสวรรค์ 2,536,875 (OAE 2,465,017)
  - หน้าที่กันพืชยืนต้นยังอยู่ที่ phenology gate + exclusion mask ตามเดิม · แก้ป้ายบนเว็บ "เฉพาะ GLAD (แม่นกว่า)" เป็น **"(จับนาไม่ครบ)"** ให้ตรงกับที่วัดได้
- **เว็บโหลดข้อมูลใหม่เต็มทุกครั้งโดยไม่จำเป็น** — ทุก fetch ตั้ง `cache: "no-store"` (ห้ามเก็บแคชเลย) **และ**ต่อท้าย `?_=Date.now()` ทำให้ URL ไม่ซ้ำเดิม แคชจึงไม่มีวันแมตช์ · เปิดเว็บทีไรดาวน์โหลดใหม่หมด รวม `rain-stations.json` 1.8 MB + `water-level.json` 830 KB
  - เปลี่ยนเป็น `cache: "no-cache"` = ยังถามเซิร์ฟเวอร์ทุกครั้งว่าข้อมูลเปลี่ยนไหม **แต่ถ้าไม่เปลี่ยนได้ 304 ไม่ต้องโหลดซ้ำ** · ตัด cache-buster ออก 10 จุด · ข้อมูลยังสดเท่าเดิมทุกประการ
  - เป็นตัวเร่งให้บั๊กจังหวะโหลดโผล่ (เช่น `eviPeriodLabel` recursion ที่เจอวันเดียวกัน) เพราะยิ่งโหลดนาน ยิ่งมีช่วงที่ข้อมูลยังไม่มา
- **ด่านคัดกรองนาข้าวเข้มเกินไป — ผ่อน `MIN_EVI_MAX` 0.20 → 0.22** — ด่านบังคับว่าพื้นที่ต้องเคยมีเดือนที่โล่งจน `EVI ≤ 0.20` (เพื่อตัดยาง/ปาล์มที่เขียวทั้งปี) แต่นาไทยหลายที่มีวัชพืช/ตอซังคลุม ไม่เคยโล่งขนาดนั้นจึงถูกตัดทิ้ง
  - **ตรวจสมมติฐานแรกแล้วผิด:** เดาว่าเป็นเพราะนาน้ำฝนอีสาน "หว่านแห้ง" ด่านจึงไม่เห็นน้ำขัง · ทดสอบโดย**ถอดเงื่อนไขน้ำขังออกทั้งหมด** ขยับแค่ 0.38 → 0.40 เท่า **ไม่ใช่สาเหตุ**
  - เลือก 0.22 จากการวัด 5 ระดับ เพราะเป็นค่าเดียวที่**ดีขึ้นทั้งค่ากลางและค่าเฉลี่ย** · ผ่อนกว่านี้พื้นที่รวมเข้าใกล้ OAE ก็จริงแต่ค่าเฉลี่ยแย่ลงเพราะปนพืชอื่น (ที่ 0.30 จังหวัดกลุ่มควบคุมที่แทบไม่มีนาพุ่งเป็น **9 เท่า**ของความจริง)
  - **ผลจริงหลังรัน** 62 จังหวัดที่เว็บแสดง: พื้นที่รวม 14.28 → 21.47 ล้านไร่ (0.33× → 0.49× ของ OAE) · **คลาดกลาง 76% → 53%** · คลาดเฉลี่ย 66% → 60% · ดีขึ้น 38/62 จังหวัด แย่ลง 24
  - *probe ทำนายไว้ดีเกินจริง (คาด 0.70× และดีขึ้น 47/61) เพราะ probe นับพิกเซลจากด่านตรงๆ ส่วนของจริงผ่านตรรกะ GLAD-preferred อีกชั้น — ทิศทางตรงแต่ขนาดไม่ตรง*
  - **ยังไม่ทำให้ตัวเลขรายจังหวัดเชื่อถือได้** (คลาดเฉลี่ยยัง 60%) ป้าย "ให้ยึด OAE เป็นหลัก" ยังต้องอยู่ · ร้อยเอ็ดกับนครสวรรค์ไม่ขยับเลย แปลว่าติดที่ตรรกะ GLAD-preferred ไม่ใช่ด่านนี้
- **แผนที่หายไปเป็นบางครั้ง — เจอ 2 สาเหตุ**
  - **`eviPeriodLabel()` เรียกตัวเองไม่รู้จบ** (บั๊กที่เพิ่งสร้างวันเดียวกัน) — ตอนเพิ่มฟังก์ชันใช้ replace ทั้งไฟล์ แล้วไปโดนบรรทัด fallback ในตัวฟังก์ชันเอง กลายเป็น `return eviPeriodLabel()` · เมื่อ `rice-evi.json` ยังโหลดไม่เสร็จหรือเป็นรูปแบบเก่าที่ไม่มี `period_start` จะ recursive จน `Maximum call stack size exceeded` แล้ว **การวาดทั้งหน้าหยุดกลางคัน แผนที่หายทั้งภาพ** · เกิดแบบสุ่มเพราะแข่งกับไฟล์ที่ใหญ่กว่า (`rain-stations.json` 1.8 MB · `water-level.json` 830 KB) และทุก fetch ตั้ง `no-store` + cache-buster จึงไม่มีแคชช่วย
  - **จังหวัดที่ไม่มีข้อมูลจางเกินไป** — `.province.faded` ตั้งไว้ `opacity: 0.15` บนพื้นสีเข้ม ทำให้แทบมองไม่เห็น และเกิด**ตามปกติ** ไม่ใช่ความผิดพลาด: layer เขื่อนมีข้อมูล 26/77 จังหวัด **อีก 51 จังหวัดจึงหายไปครึ่งประเทศ** · ปรับเป็น `0.35` (แผนที่ย่อ 0.22 → 0.35 ด้วย) เส้นขอบจังหวัดชัดขึ้น 2.3 เท่า ยังแยกออกว่าไม่มีข้อมูลแต่เห็นรูปประเทศครบ
- **ป้ายพื้นที่นาจากดาวเทียมบอกกลับทางกับความจริง + ตัวเทียบดูข้างเดียว** — ป้ายเขียนว่า *"MODIS 1km มักเกินจริง"* แต่วัดจริง 76 จังหวัด (ส.ค. 2569) พบว่า**ภาพรวมต่ำกว่า OAE ราว 3 เท่า** (13.7 ล้านไร่ เทียบ 43.9 ล้านไร่ = 0.31 เท่า · ค่ากลางรายจังหวัด 0.23 เท่า)
  - **บั๊กซ้อน:** ตัวเทียบ `overstate` เช็คเฉพาะค่าเกิน (`>2` เหลือง `>5` แดง) จังหวัดที่ดาวเทียมเห็น**ต่ำกว่าจริง**จึงขึ้นเขียวว่า "ใกล้เคียง" เสมอ — ร้อยเอ็ดเห็นแค่ **0.06×** ของ OAE (6%) แต่ขึ้นเขียว · แก้ให้วัดระยะห่างจาก 1 ทั้งสองทาง และแยกข้อความ "สูงกว่า OAE (อาจปนยาง/ปาล์ม)" กับ "ต่ำกว่า OAE (ประตูคัดกรองตัดทิ้งมาก)"
  - เจอตอนวัดว่าควรเปลี่ยนไป 250 ม. ไหม — คำตอบคือ**ไม่ควร** ใกล้ OAE กว่าแค่ 36/76 จังหวัด (47% เท่ากับเดา) และพื้นที่รวมแทบไม่ขยับ (−0.5%) เพราะคอขวดคือประตู phenology ที่ยังเป็น 1 กม. และ MCD12Q1 ที่ 500 ม. ไม่ใช่ scale ตอน reduce
- **ป้าย EVI อ้างระยะข้าวทั้งที่วัดได้แค่ความเขียวของทรงพุ่ม** — เกณฑ์ `EVI ≥ 0.40 → "ออกรวง/ออกดอก"` ใช้ไม่ได้กับ**ข้าวไวต่อช่วงแสง**ซึ่งครองพื้นที่อีสาน · ร้อยเอ็ดปลูกหอมมะลิ **99% ของนาปี** ซึ่งออกดอกตามช่วงแสงราว **20 ต.ค.** ไม่ว่าหว่านเดือนไหน ทรงพุ่มจึงปิดตั้งแต่ ก.ค. แต่ยังห่างออกรวงอีก 3 เดือน · ข้อมูล ก.ค. ขึ้น "ออกรวง" ถึง 46/72 จังหวัด
  - เปลี่ยนป้ายทั้ง `STAGE_MAP` และ `RICE_EVI_TIERS` ให้บอกสิ่งที่วัดได้: ออกรวง→**ทรงพุ่มเขียวเต็มที่** · แตกกอ→**ทรงพุ่มกำลังขยาย** · สุกแก่→**ทรงพุ่มเริ่มโรย** · ต้นกล้า→**เริ่มเขียว ทรงพุ่มบาง** · เก็บเกี่ยว→**ทรงพุ่มโล่งลง** · นาว่าง→**แทบไม่มีพืชปกคลุม**
  - ป้ายแนวโน้มตัดคำว่า "→ เข้าสู่ออกรวง" และ "→ สร้างเมล็ด/สุกแก่" ออกทั้งคู่ · **ระยะข้าวตามปฏิทินยังแสดงอยู่ในการ์ดเดิม แยกจากสิ่งที่ดาวเทียมวัด**
  - ตรวจกับแหล่งนอกก่อนแก้: กรมการข้าว (ข้าวไวแสงปลูก พ.ค.–ส.ค.) · รายงานสถานการณ์ข้าวรายสัปดาห์ 20 ก.ค. 2569 · รายงานศัตรูข้าว กรมส่งเสริมการเกษตร 27 พ.ค. 2569 · วงจร กข.105 ออกดอก ~20 ต.ค.
- **ลบ secret `TELEGRAM_BOT_TOKEN` ที่ค้างอยู่** — Telegram alert bot ถูกลบไปตั้งแต่ commit `6962415` แต่ secret ยังอยู่ · ตรวจแล้วไม่มีโค้ด/workflow ไหนอ้างถึงเลย · เหลือ 2 secret ที่ใช้งานจริง (`FIRECRAWL_API_KEY` · `GEE_SERVICE_ACCOUNT_KEY`)
- เก็บของตกค้างใน `fetch_oae_prices.py`: ค่าคงที่ `PAGE` ที่เหลือจากตอนไล่ดึงทุกหน้า และ `from datetime import` ซ้ำในฟังก์ชัน
- **[ก่อนขึ้นจริง] การ์ด "ตากข้าวได้เพิ่ม 1,200 บาท/ตัน" คำนวณผิด เกินจริง 7 เท่า** — เอาส่วนต่างราคา 15% กับ 25% มาแสดงตรงๆ โดยลืมว่า**ตากแล้วน้ำหนักหายไปด้วย** · ข้าว 1 ตันที่ความชื้น 25% ตากเหลือ 15% จะเหลือ 882 กก. (หาย 118 กก.) → ขายได้ 7,765 บาท เทียบกับขายเปียกเลย 7,600 บาท **ได้เพิ่มจริงราว 165 บาท ไม่ใช่ 1,200** และยังไม่หักค่าตาก/ค่าแรง · ตัดออก เหลือแสดงราคาสองความชื้นให้เทียบเอง
- **การ์ด EVI เอาข้อมูลคนละเดือนมาต่อกันโดยไม่บอก** — หลังเปลี่ยนปฏิทินให้ใช้วันจริง การ์ดจึงแสดง "ระยะจากดาวเทียม (มิ.ย.)" ต่อลูกศรกับ "ปฏิทิน (ส.ค.)" ราวกับเป็นเวลาเดียวกัน · ติดป้ายแยกให้ชัด: `🌾 ดาวเทียม 2026-06: ...` / `📅 ตามปฏิทิน ตอนนี้: ...` ทั้งในการ์ดรายละเอียดและ tooltip
  - ข้อสังเกตนี้นำไปสู่การไล่ตรวจตัวจำแนกระยะ EVI ทั้งประเทศ (รายการถัดไป)
- **layer กำไรบอกว่า "ไม่รวมค่าเช่านา" ทั้งที่รวมอยู่แล้ว** — ป้ายกำกับเขียนว่าต้นทุน 6,100 ฿/ไร่ ไม่รวมค่าเช่า แต่บวกองค์ประกอบทั้ง 7 รายการใน `COST_BASE` ได้ 6,100 พอดี **โดยมีค่าเช่านา 1,000 ฿/ไร่ รวมอยู่** (ข้าวหอมมะลิเช่นกัน 5,150 = ผลรวมพอดี รวมค่าเช่า 700) · เจ้าของที่ดินที่ไม่ต้องจ่ายค่าเช่าจึงถูกประเมินว่าขาดทุนเกินจริงถึง 1,000 ฿/ไร่ · แก้ป้ายให้ตรงกับตัวเลข
- **layer กำไร: เขียนที่มาให้ชัดว่าเป็นค่าเฉลี่ยประเทศ** — เดิมเขียนแค่ "ต้นทุน OAE" ซึ่งอ่านได้ว่าเป็นต้นทุนของจังหวัดนั้น · ตอนนี้ระบุตรงๆ ว่าต้นทุนเป็น**ค่าเฉลี่ยทั้งประเทศ** คู่กับ**ผลผลิตเฉลี่ยของจังหวัด** ใช้เทียบจังหวัดต่อจังหวัดได้ แต่ไม่ใช่กำไรจริงของแปลงใด (ตัดสินใจเก็บ layer ไว้เพราะแผนที่หลักเป็นเครื่องมือวิเคราะห์ คนละกลุ่มผู้ใช้กับหน้าชาวนา)

- **ราคาระดับประเทศ (สศก.) หายไปเงียบๆ 3 เดือน — และเว็บอ่านไม่เจอมาตั้งแต่แรก** — `fetch_oae_prices.py` ยิง CKAN `datastore_search` ของ catalog.oae.go.th ซึ่งตั้งแต่ **20 พ.ค. 2569** ตอบ **403 "ไม่ได้รับอนุญาตให้อ่านทรัพยากร"**
  - สคริปต์กลืน exception แล้ว**เขียน `oae_national: {}` ทับของเดิม** แต่ยัง `exit 0` → workflow เขียวทุกวัน · freshness monitor ก็ไม่จับ เพราะวัดจากอายุ commit และไฟล์เดียวกันถูกสคริปต์ราคาโรงสีเขียนวันละ 3 ครั้งอยู่แล้ว **ไฟล์สดเสมอ แต่เนื้อในว่าง**
  - **บั๊กซ้อน:** ตอนที่ยังดึงได้ สคริปต์เขียนคีย์ `white_rice` (ชื่อ dict) แต่ `index.html` อ่าน `oae_national.white` → **การ์ดราคาที่ไร่นาไม่เคยขึ้นเลยแม้แต่ครั้งเดียว** · `cfg["key"] = "white"` มีอยู่ในโค้ดแต่ไม่เคยถูกใช้
  - **แก้:** ย้ายไป API ที่ตัว catalog ชี้ไปเอง (`agriapi.nabc.go.th`) เปิดสาธารณะ ไม่ต้องมี key · ได้**รายสัปดาห์**แทนรายเดือน ย้อนถึง 2554 และมี**หอมมะลิ**ด้วย (เดิมมีแต่ข้าวเจ้า) · ล่าสุด ก.ค. 2569 สัปดาห์ 4: ข้าวเจ้า 8,377 · หอมมะลิ 16,449 บาท/ตัน
  - **cron ตัวแรกหลังแก้ขึ้นแดงทันที — ซึ่งคือจุดประสงค์** Cloudflare หน้า API ตอบ **403 ให้ IP ของ GitHub runner** (ยิงจากไทยผ่านทุก UA · ลองส่ง header แบบเบราว์เซอร์แล้วไม่ช่วย เพราะบล็อกที่ชื่อเสียง IP ดาต้าเซ็นเตอร์ ไม่ใช่ UA) · ถ้าเป็นสคริปต์เดิมจะเงียบต่อไปอีกไม่รู้กี่เดือน
  - หาแหล่งสำรองแล้วไม่มีที่ใช้ได้ — data.go.th มีชุดเดียวกันแต่หยุดที่ ธ.ค. 2568 (เก่ากว่า 8 เดือน) · จึงสลับไปใช้ **Firecrawl** เป็นทางสำรองเมื่อเจอ 403 (ใช้เบราว์เซอร์จริงจึงผ่าน · รีโปตั้ง `FIRECRAWL_API_KEY` ไว้อยู่แล้วสำหรับราคาปุ๋ย) · log บอกทุกครั้งว่าใช้ทางไหน `[direct]` หรือ `[firecrawl]`
  - ขอแค่แถวล่าสุดแถวเดียว (`limit=1`) แทนไล่ 841 แถว → เหลือ 2 คำขอต่อรอบ ประหยัดทั้งเวลาและเครดิต
  - **กันข้อมูลค้างอีกชั้น:** ถ้าแถวล่าสุดเก่ากว่า 3 เดือน ถือว่าต้นทางหยุดอัปเดต → workflow แดง แทนที่จะ "ดึงสำเร็จ" ทุกวันด้วยค่าเดิม
  - **กันไม่ให้เงียบอีก:** ดึงไม่สำเร็จ = ไม่ลบข้อมูลเดิม + ไม่ขยับ `updated_at` + `exit 1` ให้ workflow แดง (ทดสอบเส้นทางล้มเหลวแล้ว) · ตัด `trend_12m` ที่ไม่มีใครอ่านออก

- **[ตรวจแล้วไม่แก้] ระยะข้าวจาก EVI ที่ดูขัดกับปฏิทิน — สรุปว่าปฏิทินนาปีใช้ตัดสินไม่ได้** — ตั้งข้อสงสัยว่า `classify_evi()` ตัดสินจากค่า EVI + ทิศทางโดยไม่รู้เดือน ทำให้ มิ.ย. 2569 มี 5 จังหวัด (เชียงใหม่ · เชียงราย · อุตรดิตถ์ · ขอนแก่น · สมุทรปราการ) ขึ้นว่า "สุกแก่ ใกล้เก็บเกี่ยว" ทั้งที่นาปีเหนือ/อีสานเก็บเกี่ยว พ.ย.–ม.ค.
  - ทำกฎกันไปแล้วทั้งฝั่งเว็บและ pipeline **แล้วถอดออกทั้งหมด** เมื่อตรวจข้อมูลนาปรังในรีโปเอง: **เชียงราย 127% · อุตรดิตถ์ 94% · สมุทรปราการ 92% · เชียงใหม่ 70% ของพื้นที่นาปี** เป็นนาปรังชลประทาน ซึ่งปลูกเหลื่อมกันได้ตลอดปี → "สุกแก่ เดือน มิ.ย." **เป็นไปได้จริง** ปฏิทินนาปีอย่างเดียวจึงใช้ตัดสินไม่ได้
  - ข้อสงสัยถัดมาว่าเกณฑ์ค่า EVI ≥ 0.40 ให้ป้าย "ออกรวง" เร็วไป (36% ของรายการอยู่ในช่วงปลูก) **ตกไปด้วยเหตุผลเดียวกัน** — ภาคกลางมีนาปรัง 77% ของพื้นที่นาปี
  - **บทเรียน:** อย่าเอาสมมติฐาน (ปฏิทิน) ไปทับข้อมูลที่วัดได้ (EVI) เมื่อยังพิสูจน์สมมติฐานไม่ได้ · การ์ดแสดงทั้งสองด้านโดยติดป้ายชัดอยู่แล้ว ("ดาวเทียม 2026-06: …" / "ตามปฏิทิน ตอนนี้: …") ให้ผู้อ่านเห็นความต่างเอง เพียงพอแล้ว
  - **สิ่งที่เก็บไว้:** ย้าย `classify_evi()` ไป `scripts/rice_stage.py` ที่เดียว — เดิมโค้ดชุดเดียวกันซ้ำอยู่ทั้ง `fetch_rice_evi.py` และ `fetch_rice_evi_district.py` แก้ที่เดียวแล้วอีกที่ค้างได้ · มี self-test รันด้วย `python scripts/rice_stage.py`
- **[ก่อนขึ้นจริง] ตัดการ์ดระดับน้ำในเขื่อนออกจากหน้าชาวนา** — ตัวเลขถูกต้อง แต่ **ข้าวนาปีเป็นนาน้ำฝนทั้งหมด** ชาวนาส่วนใหญ่ไม่ได้ใช้น้ำจากเขื่อนนั้น เห็นแถบแดง "น้ำน้อยวิกฤต" แล้วตกใจโดยไม่เกี่ยวกับแปลงตัวเอง · ตัดทั้งแถวในการ์ดฝน คำแนะนำ `dam_low` และข้ามคำเตือนชนิด `dam` (แผนที่หลักยังมี layer เขื่อนครบ)
- **[ก่อนขึ้นจริง] ป้ายตีความปริมาณฝนขัดกับระดับเตือนของระบบเอง** — การ์ดฝนแสดง**พยากรณ์ดิบ** แล้วแปะป้ายที่ใช้เกณฑ์ของค่า**หลังหัก bias** · สุพรรณบุรีจึงขึ้นว่า "52 มม. 🟡 ฝนปานกลาง พอเหมาะกับนา" ทั้งที่ระบบเราเองประเมินว่าเหลือ 0 มม. หลังหัก 52.4 · ตัดป้ายตีความออก เหลือตัวเลขกับแหล่งที่มา ให้การ์ด "ควรทำอะไรตอนนี้" เป็นตัวตัดสินความเสี่ยงที่เดียว
- **หน้าชาวนาอ้างแหล่งข้อมูลที่ไม่ได้ใช้แล้ว** — footer ยังเขียน RID หลังตัดการ์ดเขื่อน และไม่เคยเขียน DIT ทั้งที่ใช้ข้อมูลโรงสี
- **[ก่อนขึ้นจริง] ตัดการ์ดจุดคุ้มทุนออก** — เอาต้นทุน**ค่าเฉลี่ยทั้งประเทศ** (สศก. 6,100 บาท/ไร่) มาหารด้วยผลผลิตเฉลี่ยรายจังหวัด แล้วสรุปว่า "ขาดทุน 1,861 บาท/ไร่" ที่นครศรีธรรมราช · เป็นคำตัดสินที่หนักเกินกว่าข้อมูลรองรับ และหมายเหตุกำกับก็ช่วยไม่ได้เมื่อกล่องสีแดงบอกว่าขาดทุน — ตัดทั้งบล็อกพร้อมโค้ดที่เลี้ยงมัน
- **ปฏิทินข้าวเดินตามเดือนของภาพดาวเทียม ไม่ใช่วันจริง** — `riceSeasonContext()` อ่านเดือนจาก `RICE_EVI_DATA.month` ซึ่งช้าราว 2 เดือน (วันนี้ 17 ส.ค. แต่ไฟล์ EVI เป็น 2026-06) · ช่วงนี้บังเอิญตกกรอบเดือนเดียวกันจึงยังไม่เห็นผล แต่พอถึง พ.ย. จะขึ้นว่า "ข้าวกำลังเจริญเติบโต" ทั้งที่ถึงรอบเก็บเกี่ยวแล้ว · เปลี่ยนไปใช้วันที่จริง
- **หมุดสถานีวัดน้ำไปโผล่ผิดจังหวัด 21 จุด** — ThaiWater ติดชื่อจังหวัดไม่ตรงกับพิกัดจริง ("บ้านฟ้าหยาด" ระบุยโสธรแต่พิกัดห่าง **385 กม.**, "บ้านมีชัย" ระบุหนองคายแต่ห่าง 260 กม.) ทำให้หมุดขึ้นผิดที่และการนับ "สถานีเฝ้าระวังในจังหวัดนี้" เพี้ยนตาม · ตรวจพิกัดกับกรอบจังหวัดจาก `districts-geo.json` แล้วตัดตัวที่ห่างเกิน 50 กม. ออก (เผื่อสถานีชายขอบจริง — ตัวที่เหลือห่างมากสุด 40 กม.) เป็นวิธีเดียวกับที่ใช้ตรวจ geocode โรงสี
  - **หมายเหตุแก้ความเข้าใจผิด:** เมื่อ 13 ส.ค. เคยบันทึกว่าสถานีเหล่านี้คือสถานีริมแม่น้ำโขง เพราะจังหวัด/อำเภอที่ระบุอยู่ริมโขง — **ไม่ใช่** พิกัดจริงอยู่กลางประเทศ · โปรเจกต์ยังไม่มีสถานีบนลำน้ำโขงสายหลักตามที่บันทึกไว้ในหมวดทราบปัญหา
- **596 สถานีขึ้นว่า "ไม่มีข้อมูล · ล้นตลิ่ง" พร้อมกัน** — ThaiWater ส่ง 628/1,422 สถานีมาโดยไม่มีระดับสถานการณ์ แล้วเอาค่าระดับน้ำทะเลปานกลางใส่ช่อง "ส่วนต่างจากตลิ่ง" พร้อมป้าย "ล้นตลิ่ง (ม.)" — สถานีริมโขงจึงรายงานเป็น "ล้นตลิ่ง 215 เมตร" · ตัดค่าที่เชื่อไม่ได้ทิ้ง เก็บ `msl` + ค่าก่อนหน้าไว้ดูแนวโน้มแทน และเปลี่ยนป้ายเป็น "ไม่มีเกณฑ์ตลิ่ง" เพราะมีค่าอ่านได้ ขาดแค่เกณฑ์
- **ป้าย/ตัวเลขค้างทั่วเว็บ** (จากการไล่ตรวจด้วย `/stale-check`) — layer สถานีวัดฝน/ระดับน้ำแสดงคำอธิบายของ layer ก่อนหน้าค้างไว้ (ข้อมูลเซนเซอร์จริงถูกกำกับว่า "ประมาณการจาก YoY 2565–2567") · โรงสีแสดง 903 โรง/60 จว. ทั้งที่จริง 970/61 และวันที่ผิดเดือน · วันที่ ISO ดิบโผล่ในประโยคไทย 30 จุด → ใช้ `Intl.DateTimeFormat` แปลง พ.ศ. · ลบ layer `fertilizer` ที่ข้อมูลถูกลบไปตั้งแต่ commit เก่าแต่ config ยังค้าง
- **freshness monitor ไม่ได้เฝ้า `fertilizer-prices.json`** — ต้นเหตุที่ทำให้ราคาปุ๋ยค้าง 2 สัปดาห์โดยไม่มีใครรู้ (workflow เขียว แต่ไม่มีอะไรตรวจ) · เพิ่มเข้ารายการพร้อม `alert-scoreboard.json` และเขียนเหตุผลกำกับไฟล์ที่ตั้งใจไม่เฝ้า
- **ปรับพยากรณ์ฝนให้แม่นขึ้นด้วย auto-calibration** — วัดจากสถิติสะสมพบว่าพยากรณ์ Open-Meteo over-predict อย่างเป็นระบบ (~+47มม.) ทำให้เตือน 🔴 เสี่ยงสูงผิดบ่อย (precision เดิม 12%) หักค่า bias สะสมนี้ออกจากพยากรณ์ก่อนตัดสินระดับเตือน (เฉพาะฝั่งพยากรณ์ ไม่แตะข้อมูลดาวเทียมจริง, เริ่มใช้เมื่อมีข้อมูลสะสม ≥5 windows) ลดจำนวนจังหวัด 🔴 จาก forecast-only false alarm ได้ ~40%
- **ข่าวการเมืองหลุดผ่านฟิลเตอร์ข่าวข้าว** — ดราม่าการเมือง (เช่น สส.ใส่กางเกงขาสั้น, ข้อกล่าวหาโกงสอบท้องถิ่น) ที่ใช้คำว่า "ชาวนา" เชิงวาทศิลป์ผ่านเกณฑ์เดิมเพราะมีคำตรงตัว เพิ่มคำยกเว้นเฉพาะจุด + กฎตัดข่าวที่ติดป้ายหมวด "การเมือง"
- **ราคาปุ๋ย live ซ้ำแถว** — โค้ด log อ้างคีย์เก่าที่ไม่มีแล้วหลัง refactor เป็นหลายสูตรต่อแหล่ง (`price` → `prices`) ทำให้ทุกครั้งที่ดึงสำเร็จ error แล้วตกไปเติมแถวเก่าซ้ำเข้ามาด้วย
- **[ปฏิบัติการ] ราคาปุ๋ย live ค้าง 2 สัปดาห์ (22 ก.ค. – 5 ส.ค.)** — GitHub Actions secret `FIRECRAWL_API_KEY` หายไป/ว่างเปล่า ทำให้ workflow รายสัปดาห์ no-op เงียบๆ ทุกรอบ ตั้งค่าใหม่แล้ว รีเฟรชสำเร็จ

### ตรวจสอบกับเหตุการณ์จริง (Validation)

เทียบผลของระบบกับข่าวราชการ/สื่อ เพื่อเก็บหลักฐานว่าอะไรใช้ได้จริงและอะไรยังไม่ได้
**ไม่ใช่การวัดความแม่นยำ** — เคสเดี่ยวพิสูจน์ไม่ได้ ตัวเลขที่วัดได้จริงอยู่ใน `alert-scoreboard.json`

- **17 ส.ค. — คำเตือนฝนหนัก 18–21 ส.ค. ภาคเหนือ/อีสานตอนบน** (เดลินิวส์ อ้างกรมอุตุฯ) · พยากรณ์เราครอบคลุม 16–22 ส.ค. ตรงหน้าต่าง · ขึ้น 🔴 ภาคเหนือ 3/9 (น่าน 268, แม่ฮ่องสอน 220 มม.) อีสานตอนบน 4/9 (**บึงกาฬ 320, นครพนม 272 มม.**) ตรงกับจังหวัดที่ข่าวรายงานความเสียหาย 67,335 ไร่
  - **ข้อมูลโขงที่เพิ่งเพิ่มยืนยันได้ทันที** — ข่าวระบุโขงที่นครพนม "เกือบ 12 เมตร" สูงนานนับสัปดาห์ · MRC ให้ 11.575 ม. (วิกฤต 12.0) 🟠 · กลไกที่ข่าวชี้ว่าเป็นต้นเหตุ ("โขงหนุน + ลำน้ำสาขาระบายช้า") เมื่อ 3 วันก่อนระบบยังมองไม่เห็นเลย
  - **ฐานการเตือนแคบลงจริง** จาก 87% ของประเทศ (12 ส.ค.) เหลือ 56% (16 ส.ค.) และ 🔴 จาก 21 → 11 จังหวัด · แต่**ยังไม่ใช่หลักฐานว่าแม่นขึ้น** อาจเป็นเพราะฝนน้อยลงจริง ต้องรอ scoreboard เทียบฝนที่ตกจริง
- **8–15 ส.ค. — ตรวจกับข่าวเตือนภัยอีก 4 ฉบับ** (ปภ. 6 จังหวัดอันดามัน · วอร์รูมน้ำท่วมเหนือ · 5 จังหวัดริมโขง · น้ำโขงหนุน 4 จังหวัด) · จังหวัดที่ข่าวระบุติดเตือนครบทุกครั้ง แต่**ช่วงนั้นระบบเตือน 73–87% ของประเทศทุกวัน** การ "ตรงครบ" จึงไม่ใช่หลักฐานว่าชี้เป้าแม่น — เป็นที่มาของงาน calibration และการเปลี่ยนป้ายจากนับจังหวัด → จัดอันดับความรุนแรง
  - การตรวจรอบ 15 ส.ค. ทำให้เจอบั๊กหมุดผิดจังหวัด 21 จุด และพบว่าเราไม่มีสถานีโขงเลย → นำไปสู่การเพิ่มข้อมูล MRC
  - *(เลขฐานรายวันดูได้จาก `git log data/agri-warnings.json` — ตัวเลขรายเคสที่เคยสรุปในแชตไม่ได้บันทึกไว้ จึงไม่ยกมาอ้างซ้ำ)*

### ทราบปัญหา / รอดำเนินการ (Known issues)
- **พื้นที่นาจากดาวเทียมยังเชื่อรายจังหวัดไม่ได้** — หลังผ่อนด่าน phenology และถอดกฎ GLAD-preferred แล้ว ดีขึ้นมาก (0.33× → 0.70× ของ OAE · คลาดกลาง 76% → 43%) แต่คลาดเฉลี่ยยัง 51% · ตัวเลขที่ใช้ตัดสินใจจริงควรยึด OAE
- **ตัดสินใจแล้ว: ไม่เปลี่ยน reduce scale เป็น 250 ม.** — วัดแล้วว่า GEE รับไหว (พิกเซลเพิ่ม 15.5 เท่า เวลาเพิ่มแค่ 1.4–1.6 เท่า) แต่ไม่ทำให้แม่นขึ้น จึงไม่คุ้มความเสี่ยงที่ตัวเลขพื้นที่ทุกจังหวัดจะเปลี่ยน
- **ตัดสินใจแล้ว: ไม่ยุบ 18 layer** — เคยเสนอให้ยุบ (โดยเฉพาะ 8 layer เรื่องน้ำที่ซ้ำหัวข้อกัน) แต่เจ้าของโปรเจกต์เลือกคงไว้ทั้งหมด · ข้อเสนอเดิมมาจากหลักการล้วน ไม่มีข้อมูลว่า layer ไหนถูกใช้จริง จึงไม่ใช่เหตุผลที่หนักพอจะตัดของที่ใช้งานได้อยู่
- ~~**เกณฑ์เตือนภัยเป็นค่าสัมบูรณ์ ไม่เทียบค่าปกติของพื้นที่**~~ — **แก้แล้ว 20 ส.ค. 69** ดูหมวด "เปลี่ยนแปลง" ด้านบน (แผน A: ทวีคูณ 2.0x/1.0x/0.5x ของค่าปกติรายจังหวัด แทน 30/60/120 มม. ตายตัว)
- ~~**ไม่มีสถานีวัดบนลำน้ำโขงสายหลัก**~~ — **แก้แล้ว** ด้วยข้อมูล MRC (ดูหมวดเพิ่ม) · ยังเหลือข้อจำกัด: 2 สถานี (อำนาจเจริญ, เขมราฐ) ต้นทางไม่ให้เกณฑ์วิกฤต จึงบอกได้แค่ระดับ ไม่บอกว่าวิกฤตหรือยัง

### ลบออก (Removed)
- **Telegram alert bot** — ถอดออกทั้งหมด (workflow + `telegram_notify.py` + `test_telegram_notify.py` + การ์ดสมัครใน farmer.html) · ไม่เคยใช้งานได้จริงเพราะ secret `TELEGRAM_BOT_TOKEN` ไม่เคยถูกตั้ง และ workflow ถูกปิดมาตั้งแต่ ~22 ก.ค. · ตัดทิ้งดีกว่าเก็บโค้ดที่รันไม่ได้ไว้ให้เข้าใจผิดว่ามีฟีเจอร์นี้

---

## กรกฎาคม 2569

### เพิ่ม (Added)
- **👨‍🌾 farmer.html — มุมมอง "จังหวัดฉัน" สำหรับชาวนา** — หน้าเบาแยกจากแผนที่หลัก โหลดเฉพาะข้อมูลที่จำเป็น การ์ด: คำแนะนำ, สภาพอากาศ (แปลเป็นภาษาชาวบ้าน), เงินช่วยเหลือ/มาตรการรัฐ, โรงสี/โรงไฟฟ้าชีวมวลใกล้เคียง
- **📢 Telegram alert bot** — สมัครรับแจ้งเตือนเมื่อระดับความเสี่ยง (น้ำท่วม/แล้ง) ของจังหวัดตัวเองเปลี่ยน แจ้งเฉพาะตอนเปลี่ยนระดับ ไม่สแปม *(ถอดออกแล้ว ส.ค. 2569 — ดูหมวด "ลบออก" ด้านบน)*
- **💰 ราคาปุ๋ยเคมีจริง (ราคาซื้อขายจริง)** — แทนตารางเพดานแนะนำด้วยราคาจริงจากประกาศพาณิชย์จังหวัด + แหล่ง live รีเฟรชอัตโนมัติทุกสัปดาห์ (สหกรณ์ท่าบ่อ, svpolysack) ผ่าน Firecrawl โชว์ได้หลายสูตรต่อแหล่ง ไม่ใช่แค่ยูเรีย เพิ่มพื้นที่ภาคเหนือ (เชียงราย)
- **🗃 คลังข่าวข้าวสะสม** — เก็บประวัติข่าวที่เคยแสดง (หัวข้อ+ลิงก์) ดูย้อนหลังได้ ไม่ทับของเก่า
- **🎯 การ์ดความแม่นระบบเตือน (Forecast Accuracy)** — วัดความแม่นพยากรณ์ฝน 7 วันเทียบฝนจริงจากดาวเทียม GSMaP จับคู่เฉพาะช่วงวันที่ตรงกันเป๊ะ (heavy precision/recall, any accuracy, MAE/bias) ซ่อนจนกว่า window แรกครบกำหนด (#30)
- **🔗 Deep link URL** — แชร์สถานะแผนที่ผ่าน hash (`#layer=…&rice=…&year=…&reg=…&sel=…`) โหลดกลับได้ตรงจุด เขียน default ออกให้ URL สะอาด (#29)
- **🔍 ตัวตรวจความสดข้อมูล** — `check_data_freshness.py` + workflow รายวัน เทียบอายุ git-commit ของแต่ละไฟล์กับเกณฑ์ (~3× รอบ cron) ไฟล์ค้าง = fail + อีเมลเตือนจาก GitHub (#28)
- **⚡ Layer โรงไฟฟ้าชีวมวล** — choropleth 79 โรงทั่วประเทศจากข้อมูล พพ. (DEDE) ปี 2565 พร้อมรายชื่อโรง กำลังผลิต (MW) และธงว่ารับซื้อฟาง/แกลบไหม (#6)
- **🔗 การ์ดตลาดรับซื้อฟาง/แกลบ** — จับคู่ฟางเหลือใช้ของจังหวัดกับโรงไฟฟ้าชีวมวลที่รับซื้อ (ในจังหวัด → ในภาค → เตือนถ้าไม่มีในภาค) (#7, #9)
- **🌾 Layer เนื้อที่เก็บเกี่ยวข้าวนาปรัง** — ต่อจาก layer ผลผลิตนาปรัง ใช้ข้อมูล OAE ปี 2565–2568 (#14)
- **📰 การ์ดข่าวข้าวล่าสุด** — ข่าวข้าวไทยจาก Google News RSS (กรองพืชอื่น/อาหาร/สำนักแปลอัตโนมัติ, dedupe, 8 ข่าวใหม่สุด) แสดงใต้การ์ดสภาพอากาศ กล่องสูง ~2 ข่าวเลื่อน scroll ได้ อัปเดต 3 ครั้ง/วัน (#20, #21, #22)
- **📚 DATA_SOURCES.md** — เอกสารอธิบายทุก layer: ใช้ข้อมูลอะไร คำนวณอย่างไร พร้อมลิงก์ตรงไปหน้า/endpoint/ไฟล์ที่ระบบดึงจริง (OAE PDFs, API ราชการ, GEE datasets, DOI งานวิจัย) (#23, #24)
- **🤝 ระบบทำงานร่วมหลาย agent** — `AGENTS.md` (คู่มือ repo ใช้ร่วม Codex/Claude) + `HANDOFF.md` (work log + โปรโตคอลสลับ agent) โดย Codex

### เปลี่ยนแปลง (Changed)
- **ปุ่ม "หน้าชาวนา" → "สำหรับชาวนา"** — ชัดเจนขึ้นว่าใครใช้
- **ถอดการ์ดความแม่นระบบเตือนออกจาก sidebar** — ข้อมูลตอนนั้นมี window น้อยเกิน (ตัวเลขยังไม่นิ่ง) ถอด UI ออกชั่วคราว แต่ backend (`score_alerts.py`) ยังรันเก็บสถิติต่อเนื่องทุกวันไว้ใช้ภายหลัง
- **พยากรณ์ฝนแม่นขึ้นด้วย multi-point p90** — เปลี่ยนจากวัดฝนที่ centroid จุดเดียวต่อจังหวัด เป็นสุ่ม ≤6 จุดกระจายในเขตจังหวัดแล้วสรุปด้วย p90 → จับฝนกระจุกเฉพาะจุด (orographic แถบเทือกเขา เช่น กาญจนบุรี/ตาก) ที่เดิมมองไม่เห็น โดยไม่ตื่นตูมเท่า max (#18)
- **ซ่อนปุ่มกำไร/ขาดทุน + จัดปุ่มชั้นข้อมูลเป็นกริดสมมาตร** (#9)
- **ล้างโค้ด/ไฟล์รก (Codex)** — ลบสคริปต์เก่า ไฟล์ scratch/docx ที่ track ผิด, แทน credit injection แบบ obfuscate ด้วย element ธรรมดา, รวม 5 data loader เป็น `fetchLayerData` (−2,512 บรรทัด)
- **ponytail-review cuts** — percentile เขียนเอง → stdlib `statistics.quantiles` (พิสูจน์ค่าตรงกัน 4,500 เคส), ตัด field `n_pts`/`icon` ที่ไม่มีอะไรอ่าน, ลบ dead code (#26)
- **ponytail-audit ทั้ง repo** — ลบ dead scripts `extract_pdf_prices.py` (workflow ที่เล็งไม่เคยมี) + โฟลเดอร์ drop-PDF ที่ตายทั้งเส้น, `fetch_rice_mills_api.py` (เส้นทาง DIT-API ที่ยกเลิกไปใช้ Excel แล้ว) — รวม −338 บรรทัด
- **กู้ scripted regeneration ข้อมูล 2568** — สคริปต์ parse ข้อมูลทางการปี 2568 (`extract_oae.py` + `update_rice_data.py` + `data/oae_extracted.json`) เคยถูกลบตอน cleanup ทำให้ dataset regenerate ไม่ได้ครบ · กู้กลับ ทำ portable เขียน rice-data.csv+js sync กัน · พิสูจน์ reproduce csv ได้ byte-for-byte
- **เอกสาร pipeline ข้าวนาปีถูกต้อง** — AGENTS.md/DATA_SOURCES.md เดิมชี้ builder ผิดตัว · แก้ให้ตรงลำดับจริง (build_rice_dataset → update_rice_data → estimate_2568_2569 → clear_estimated_trend_prices)

### แก้ไข (Fixed)
- **ฟิลเตอร์ข่าวข้าวเข้มขึ้นอีกชั้น** — ตัดข่าวหวย/บันเทิง/สำนักแปลต่างประเทศที่ยังหลุดผ่านเกณฑ์เดิม
- **Telegram workflow ล้มทุกรอบ** — `git add` ไฟล์ที่ยังไม่ถูกสร้าง (ตอน `TELEGRAM_BOT_TOKEN` ยังไม่ตั้ง) ทำให้ commit step fail ทุกครั้ง
- **นาปรัง + หอมมะลิ = แผนที่ว่าง** — ข้าวหอมมะลิเป็นข้าวไวแสง ปลูกเฉพาะนาปี ไม่มีนาปรัง · กดปุ่ม layer นาปรังตอนเลือกหอมมะลิ ตอนนี้สลับเป็นข้าวขาวอัตโนมัติ (นาปรัง = ข้าวเจ้าไม่ไวแสง รวมทุกพันธุ์) + โน้ตอธิบายในตารางสรุปรายภาคกันคิดว่าเป็นบั๊ก (#15)
- **regionOf() เตือนเมื่อเจอชื่อจังหวัดไม่รู้จัก** — แทน fallback เป็นภาคกลางเงียบๆ (#9)
- **AGENTS.md ข้อมูลเก่า** — แก้จำนวนบรรทัด/workflow, ระบุว่ามีข้อมูลนาปรังจริง, อัปเดตรายการ 23 data files + 18 layers (#25)

---

## มิถุนายน 2569

### เพิ่ม (Added)
- **🧮 แท็บจำลอง (Simulator)** — จำลองกำไร/ขาดทุนต่อไร่ในการ์ดรายละเอียดจังหวัด (ปรับราคา/ผลผลิต/ต้นทุน)
- **🌾 Layer ฟางข้าว + ผลผลิตข้าวนาปรัง** — พร้อมฟางเหลือใช้ (พลังงาน/เสี่ยงเผา) ใน tooltip
- **ตารางสรุปรายภาค** — บนแผนที่สำหรับ layer ผลผลิต/ผลผลิตต่อไร่/เนื้อที่/นาปรัง/ครัวเรือน/ฟาง
- **🌧️ Layer สถานีฝน Realtime** — ThaiWater rain_24h
- **💧 Layer ระดับน้ำในแม่น้ำ/คลอง** — สถานีวัดจาก ThaiWater (#3)
- **การ์ด Weather Watch** — สรุปพายุ/มรสุมและสภาพอากาศสำคัญ
- **ตัวชี้การปล่อยน้ำเขื่อน (dam outflow)** ใน layer ระดับน้ำในเขื่อน

### เปลี่ยนแปลง (Changed)
- **โค้ดคุณภาพ** — เพิ่ม `"use strict"`, จัดรูปแบบด้วย prettier, แก้บั๊กจาก code review
- **จำกัดช่วงปี layer นาปรัง** เป็น 2565–2568 (ยังไม่มีข้อมูล 2569)

---

## พฤษภาคม 2569

### เพิ่ม (Added)
- **🔬 Layer ความเสี่ยงโรค/แมลง** (disease & pest risk index)
- **💧 Layer ความชื้นในดิน** — NASA SMAP พร้อมคำแนะนำพืชที่เหมาะ
- **🚨 Layer พื้นที่เสี่ยงน้ำท่วม** (agri-warnings) — สังเคราะห์จากพยากรณ์ฝน + ดาวเทียม + ระดับเขื่อน
- **🌦 Layer พยากรณ์ฝน 7 วันข้างหน้า** (Open-Meteo)
- **🛰 Layer ปริมาณฝนรวม 7 วัน** — JAXA GSMaP v8 ดาวเทียมผ่าน Google Earth Engine
- **🌿 Layer ดัชนีพืชพรรณ (NDVI)** — NASA MODIS อัปเดตรายเดือน
- **🌾 Layer สภาพนาข้าว (Rice EVI)** + ปฏิทินฤดูกาล + drill-down รายอำเภอ

### สถาปัตยกรรม (Infrastructure)
- **`scripts/riceutils.py`** — รวมโค้ดที่ใช้ร่วมกันของ fetch scripts + แก้บั๊ก centroid
- **layerMeta config-driven** — รวม ladder ของ S.layer เข้า config ลดโค้ดซ้ำ
- **batch weather scripts** — ลด API requests จาก 77→2 และ 385→10

---

## เมษายน 2569 — จุดเริ่มต้น

### เพิ่ม (Added)
- **แผนที่ข้าวรายจังหวัด** — SVG choropleth 77 จังหวัด, layer พื้นฐาน (ผลผลิต/ผลผลิตต่อไร่/เนื้อที่/ราคา) จากข้อมูล OAE
- **วิดเจ็ตราคาสด** — ราคาโรงสี (สมาคมโรงสีข้าวไทย, auto-scrape PDF) + ราคาส่งออก FOB (TREA)
- **ราคาปุ๋ยเคมี** — MOC national fertilizer summary
- **👨‍🌾 Layer ครัวเรือนเกษตรกร** — OAE 2566
- **🌧 Layer ปริมาณฝน/ดัชนีแล้ง** รายจังหวัด
- **GitHub Actions crons** — อัปเดตราคา/สภาพอากาศ/น้ำอัตโนมัติ

---

## แหล่งข้อมูล (Data Sources)

| ด้าน | แหล่ง |
|------|-------|
| ผลผลิต/เนื้อที่/ราคา | สำนักงานเศรษฐกิจการเกษตร (OAE) |
| ราคาโรงสี | สมาคมโรงสีข้าวไทย |
| ราคาส่งออก | สมาคมผู้ส่งออกข้าวไทย (TREA) |
| ฝนพยากรณ์/ย้อนหลัง | Open-Meteo · JAXA GSMaP (GEE) |
| ระดับน้ำ/เขื่อน | ThaiWater (สสน.) · กรมชลประทาน (RID) |
| ความชื้นดิน/พืชพรรณ | NASA SMAP · MODIS · Sentinel-2 (GEE) |
| โรงไฟฟ้าชีวมวล | กรมพัฒนาพลังงานทดแทนฯ (DEDE) |

---

_สร้างโดย Sponlapat · แก้ไขล่าสุด: 13 สิงหาคม 2569_

> **ทุกการเปลี่ยนแปลงต้องบันทึกที่นี่** — ดูกติกาใน [AGENTS.md](AGENTS.md#important-constraints)
